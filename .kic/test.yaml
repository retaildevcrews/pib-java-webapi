###
# build commands
###

commands:
- name: test
  short: Run integration and load tests
  scriptCommands:
    - name: load
      short: Run 30 second load test
      path: test/load

    - name: integration
      short: Run integration test
      path: test/integration
